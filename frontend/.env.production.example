# Production — API public (npm run build / npm run start)
VITE_API_URL=http://<api-host>:3001
# Host được phép mở FE (tùy chọn, phân cách bằng dấu phẩy)
# VITE_ALLOWED_HOSTS=<fe-host>,157.10.198.41,localhost
