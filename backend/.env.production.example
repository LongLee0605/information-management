# Production — server UIT (chỉ dùng trên server, npm run be:prod)
# Tạo file: npm run setup:prod  (một lần, không ghi đè)
DB_HOST=uit-01.qasystem.com.vn
DB_PORT=1433
DB_NAME=QLTT
DB_USER=<sql-user>
DB_PASSWORD=<sql-password>
DB_ENCRYPT=false
DB_TRUST_SERVER_CERT=true

API_PORT=3001
CORS_ORIGIN=http://uit-01.qasystem.com.vn:1111,http://localhost:1111
